/**
 * A valid type that can be assigned to entities.
 * @ID identificaiton
 * @coreComponentObjectName A name derrived from the
 * core component object.
 */

export default interface IEType {
    id: string;
    coreComponentObjectName: string;
}