import IEType from "./IEType";

/**
 * A list of types, determined by users of this library, that an entity can be designated as.
 * @types the type list.
 */

export default interface IETypeList {
    type: IEType[]
}