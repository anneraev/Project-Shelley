/**
 * A valid type that can be assigned to components of entities.
 * @id identificaiton
 * @objectTypeName The name of the object the component represents.
 */

export default interface ICType {
    id: string;
    objecTypetName: string;
}