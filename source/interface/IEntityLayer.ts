import IEntity from './IEntity';

/**
 * Represents the central repository of all Entities and the functions that manipulate them.
 */
export default interface IEntity_Layer {
    entities: IEntity[];
}