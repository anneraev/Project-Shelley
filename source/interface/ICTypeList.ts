import ICType from "./ICType";

/**
 * A list of types, determined by users of this library, that an component can be designated as.
 * @types the type list.
 */

export default interface ICTypeList {
    type: ICType[]
}